import React from 'react';
import styled from 'styled-components';
import Box from '@material-ui/core/Box';

const TextContainer = styled.div`
  outline: 0;
`;

const Landing: React.FunctionComponent = () => <>Landing Page</>;

export default Landing;
